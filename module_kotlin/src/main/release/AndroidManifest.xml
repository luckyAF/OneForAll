<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.luckyaf.oneforall.kotlin" >
    <application
        android:allowBackup="false"
        android:icon="@mipmap/common_ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/CommonAppTheme">

        <activity android:name=".mvp.ui.activity.TasksActivity"
            android:theme="@style/CommonAppTheme.OverlapSystemBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.ui.activity.AddEditTaskActivity"/>
    </application>
</manifest>
